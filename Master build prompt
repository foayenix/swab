# SANKO VISION DEMO — MASTER BUILD PROMPT

## Instructions

This repo contains a README.md and 6 phase files that describe how to build the Sanko Vision Demo — a fully interactive React app demonstrating a traditional medicine platform.

**Read the README.md first.** It explains the build order and file structure.

Then execute ALL 6 phases in sequence:

1. Read `Phase1_Setup_Data_Shell.md` → Execute everything it says → Verify `npm run dev` works → Commit
2. Read `Phase2_Home_View.md` → Build HomeView.jsx → Integrate into App.jsx → Commit
3. Read `Phase3_Practitioner_View.md` → Build PractitionerView.jsx → Integrate into App.jsx → Commit
4. Read `Phase4_Researcher_View.md` → Build ResearcherView.jsx → Integrate into App.jsx → Commit
5. Read `Phase5_Regulator_View.md` → Build RegulatorView.jsx → Integrate into App.jsx → Commit
6. Read `Phase6_Public_View_Polish.md` → Build PublicView.jsx → Integrate into App.jsx → Final checks → Commit

## Rules

- Read each phase file COMPLETELY before writing any code for that phase
- After each phase, verify the app still runs with `npm run dev` (just start it briefly to check for errors, then move on)
- Commit after each phase with the commit message specified in that phase file
- Do NOT skip ahead — finish one phase before starting the next
- If a phase file says "do NOT modify other views" — follow that instruction
- The data layer (src/data.js) is created in Phase 1 and shared across ALL views — do not duplicate data

## After all 6 phases

Run `npm run build` to verify the production build compiles. Report what you built:

1. List every file created
2. Confirm which interactions work (there are 10 mandatory ones listed in Phase 6)
3. Note anything you couldn't implement or had to simplify
4. Tell me how to deploy this (Netlify or Vercel steps)

Do NOT ask me questions. Read the phase files — they contain everything you need. Start now with Phase 1.
